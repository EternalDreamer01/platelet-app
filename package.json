{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
	"docker:build0": "docker build --rm --target runtime -t platelet .",
	"docker:build": "npm run docker:build0 -- --check && npm run docker:build0 && notify-send \"Platelet docker build\" \"Docker build finished with exit code $?\"",
	"docker:run": "docker run --rm -e DISPLAY=$DISPLAY -v./data:/root/platelet:rw -v /tmp/.X11-unix:/tmp/.X11-unix --device /dev/dri --cpus=16 platelet",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "tauri": "tauri"
  },
  "dependencies": {
    "@heroicons/vue": "^2.1.3",
    "@nuxt/ui": "^2.16.0",
    "@pinia/nuxt": "^0.5.3",
    "@tauri-apps/api": "^1.5.6",
    "@tauri-apps/cli": "^1.5.14",
    "nuxt": "^3.11.2",
    "pinia": "^2.2.1",
    "vite": "^5.2.12",
    "vue": "^3.4.27",
    "vue-router": "^4.3.2"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.12.0"
  }
}
